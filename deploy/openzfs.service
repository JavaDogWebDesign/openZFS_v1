[Unit]
Description=OpenZFS Manager API
After=network.target zfs.target
Wants=network-online.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/opt/openzfs-manager
Environment=OPENZFS_DATABASE_URL=sqlite+aiosqlite:////opt/openzfs-manager/data/openzfs.db
Environment=OPENZFS_CORS_ORIGINS=[]
ExecStart=/opt/openzfs-manager/venv/bin/uvicorn backend.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

# Security hardening
ProtectSystem=strict
ReadWritePaths=/opt/openzfs-manager/data /etc /home /var/lib/samba /run/samba /mnt
ProtectHome=false
NoNewPrivileges=false
PrivateTmp=true

[Install]
WantedBy=multi-user.target
